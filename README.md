# catVSdog
This project implements a machine learning model to classify images of cats and dogs. It uses a dataset of labeled images, applies preprocessing techniques (resizing, normalization, encoding), and trains a deep learning model to achieve accurate predictions.
